import styles from "./template.module.css";

const Template = () => {
  return (
    <div className={styles["container"]}>
      <p className={styles["my-paragraph"]}>Template component</p>
    </div>
  );
};

export default Template;
